# kehdo web — environment variables example
# Copy to .env.local for development. Never commit real secrets.

# Resend (waitlist email)
# Get the API key from https://resend.com/api-keys
RESEND_API_KEY=re_...
# Create an Audience at https://resend.com/audiences and copy its ID
RESEND_AUDIENCE_ID=

# Plausible analytics (privacy-first, no cookies)
# Leave empty to disable analytics. Set to your domain to enable.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=kehdo.app

# API endpoint
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/v1
