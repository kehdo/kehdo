<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network security policy. Allows plaintext HTTP to the
  three loopback hosts a developer typically points the app at:

    10.0.2.2     — Android emulator's host loopback
    localhost    — physical device with `adb reverse tcp:8080 tcp:8080`
    127.0.0.1    — same as above, raw IP form

  Release builds use the platform default (cleartext denied) — see the
  main AndroidManifest.xml. Do NOT add public hostnames here.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
